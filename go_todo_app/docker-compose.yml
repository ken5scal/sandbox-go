version: "3.9"
# ref: https://docs.docker.com/compose/compose-file
services:
  app:
    image: gotodo
    environment:
      TODO_ENV: dev
      PORT: 8080
    build:
      args:
        - target=dev
    volumes:
      - .:/app
    ports:
      - "18000:8080"